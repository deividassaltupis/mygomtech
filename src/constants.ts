export enum Routes {
  Login = '/login',
  Users = '/items',
  WrongEmails = '/items/wrong',
  ReusedEmails = '/items/reused',
  Root = '/',
}

export enum API {
  Login = 'api/login',
  Verify = 'api/verify',
  Logout = 'api/logout',
  Items = 'api/items',
  User = 'api/user',
}
